[pytest]
; Mirrors `uvicorn app.main:app --app-dir backend` - backend/ needs to be on
; sys.path so `from app import ...` resolves the same way it does at runtime.
pythonpath = backend
testpaths = tests
